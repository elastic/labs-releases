[build-system]
requires = ["setuptools"]

[project]
name = "nightMARE"
version = "0.13.1"
dependencies = [
    "unicorn",
    "lief",
    "capstone==5.0.0",
    "construct",
    "yara-python",
    "requests",
    "smda",
    "pycryptodome",
    "dotnetfile",
]
requires-python = ">=3.10"
