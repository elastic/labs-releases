[build-system]
requires = ["setuptools"]

[project]
name = "nightMARE"
version = "0.9.2"
dependencies = [
    "unicorn",
    "lief",
    "capstone",
    "construct",
    "yara-python",
    "requests",
    "smda",
    "pycryptodome",
    "dotnetfile",
]
requires-python = ">=3.10"
