[build-system]
requires = ["setuptools"]

[project]
name = "nightMARE"
version = "0.7.1"
dependencies = [
    "unicorn",
    "lief",
    "capstone",
    "construct",
    "yara-python",
    "requests",
    "smda",
    "pycryptodome",
]
requires-python = ">=3.10"
